import { Routes } from '@angular/router';
import { ComponentsDemoComponent } from '../samples/components-demo.component';

export const routes: Routes = [
  { path: 'components', component: ComponentsDemoComponent  }
];
